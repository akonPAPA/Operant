# Domain Contracts

Future location for OpenAPI schemas, JSON schemas, DTO contracts, generated clients, and compatibility tests between core-api, web-dashboard, AI worker, and connectors.

Stage 1 intentionally does not overbuild this package.
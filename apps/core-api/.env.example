# Local Core API demo settings.
# The backend demo does not require production secrets, Telegram tokens, LLM keys, ERP credentials, or payment keys.

CORE_API_PORT=8080
ORDERPILOT_TENANT_HEADER=X-Tenant-Id
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/orderpilot
SPRING_DATASOURCE_USERNAME=orderpilot_app
# Set SPRING_DATASOURCE_PASSWORD locally only. Do not commit real credentials.

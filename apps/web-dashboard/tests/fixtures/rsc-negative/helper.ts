// Reaches a browser transport/authority module — the forbidden target for the F14 guard.
import { getIntakeDocuments } from "@/lib/intake-api";
export function loadViaHelper() {
  return getIntakeDocuments;
}

name: Frontend

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 0

      - id: filter
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            base="${{ github.event.pull_request.base.sha }}"
            head="${{ github.event.pull_request.head.sha }}"
          else
            base="${{ github.event.before }}"
            head="${{ github.sha }}"
          fi

          if [[ -z "$base" || "$base" =~ ^0+$ ]]; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          changed_files="$(git diff --name-only "$base" "$head")"
          printf '%s\n' "$changed_files"

          if printf '%s\n' "$changed_files" | grep -Eq '^(apps/web-dashboard/|\.github/workflows/frontend\.yml$)'; then
            echo "frontend=true" >> "$GITHUB_OUTPUT"
          else
            echo "frontend=false" >> "$GITHUB_OUTPUT"
          fi

  # Required branch-protection check: "Frontend / build".
  # This job MUST always run and reach a terminal success/failure conclusion so it is
  # never reported as skipped/neutral. When there are no frontend changes it satisfies
  # the required check with a no-op step; the real build runs only when frontend=true.
  build:
    needs: changes
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web-dashboard
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: No frontend changes (required check satisfied)
        if: needs.changes.outputs.frontend != 'true'
        run: echo "No frontend changes detected; Frontend required check satisfied."

      - uses: actions/setup-node@v6
        if: needs.changes.outputs.frontend == 'true'
        with:
          node-version: "22.18.0"
          cache: npm
          cache-dependency-path: apps/web-dashboard/package-lock.json

      - if: needs.changes.outputs.frontend == 'true'
        run: npm ci
      - if: needs.changes.outputs.frontend == 'true'
        run: npm run lint
      - if: needs.changes.outputs.frontend == 'true'
        run: npm run typecheck
      - if: needs.changes.outputs.frontend == 'true'
        run: npm run build
      - if: needs.changes.outputs.frontend == 'true'
        run: npm test
